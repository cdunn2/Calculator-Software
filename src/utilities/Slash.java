package utilities;

import java.awt.event.*;

//this is temporary until we create the GUI
public class Slash {

	
	public void slashListener(ActionEvent event)
	{
		//this should probably be done when an operand is sent to the display
		
		if (event.toString() == "/")
		{
			//get the preceeding number
			//get the number after(if this takes place after this is typed)
			//create a fraction object
			
		}
	}
}
